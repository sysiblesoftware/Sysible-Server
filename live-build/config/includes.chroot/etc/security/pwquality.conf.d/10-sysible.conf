# Sysible Server password-quality floor, enforced via pam_pwquality
# (libpam-pwquality) on `passwd` and account creation. Moderate, not draconian —
# a network-reachable server must not accept trivial passwords.
minlen = 12
minclass = 3
maxrepeat = 3
gecoscheck = 1
enforcing = 1
